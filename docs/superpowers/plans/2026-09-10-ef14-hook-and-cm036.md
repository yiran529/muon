# EF14 Hook and CM036 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an explicitly selectable EF14 ARC DDP-hook mode, independently validate EF21M/EF14 recurrences, and queue one GPT-60M EF14 run after CM035 succeeds.

**Architecture:** Keep the existing EF21M path as the default and add `arc_error_feedback=ef14` only for DDP-hook synchronization. EF14 stores rank-local residuals, compresses `gradient + residual`, updates residual with the unselected rows, and gives AdamW the averaged sparse compressed tensor. CM036 reuses CM035's paper AdamW recipe and dense baseline and runs only the missing EF14 cell.

**Tech Stack:** Python, PyTorch DDP/Futures, pytest/Gloo, YAML, Bash/torchrun.

**Spec:** User-approved staged design in chat on 2026-09-10.

## Global Constraints

- Work directly on `/home/wyr/dion`; do not create a worktree.
- Do not alter the behavior of default `ef21m` or the active CM035 process.
- Wait until CM035's ARC Python process has started before modifying imported production files.
- EF14 is accepted only with `arc_sync_mode=ddp_hook`.
- CM036 uses CM035's LR `0.001`, Adam betas `(0.9, 0.999)`, epsilon `1e-8`, clip norm `1.0`, 1000-step warmup, cosine decay, weight decay `0`, all-2D ratio `0.2`, rank `4`, seed `42`, compression after step `1000`, and bucket cap `160 MiB`.
- The detached queue must launch CM036 only if CM035 exits zero and the tested EF14 commit matches repository HEAD.

---

### Task 1: Independent recurrence oracles

- [x] Add a fixed-gradient/fixed-support multistep EF21M oracle.
- [x] Run EF21M primitive and two-rank Gloo reference tests.
- [x] Add EF14 oracle and confirm RED because EF14 primitives do not exist.

### Task 2: EF14 primitive, hook state, and checkpoint

- [x] Add explicit validated `error_feedback` configuration with `ef21m` default.
- [x] Implement collective-free EF14 prepare/finalize primitives.
- [x] Route sparse/full-support hook behavior by EF mode without changing EF21M output.
- [x] Serialize only EF14 rank-local residual state and reject cross-mode checkpoints.
- [x] Add hook, two-rank Gloo, optimizer-state, and checkpoint continuation tests.

### Task 3: CM036 queued paper-recipe run

- [x] Start a detached queue bound to CM035 PID/start identity and successful controller exit.
- [x] Add the CM036 EF14 YAML and single-cell launcher using CM035's selected physical batch/GA.
- [x] Run focused and broad relevant regressions plus a short real NCCL smoke.
- [ ] Commit the tested implementation and write its exact commit to `/tmp/cm036-ef14-ready`.
- [ ] Stop after confirming the queue remains installed; do not monitor CM035 or CM036 results.
